version: 2
jobs:
  build:
    branches:
      only:
        - master
    working_directory: /tmp
    docker:
      - image: debian:buster
    steps:
      - checkout
      - run: sudo apt-get install hugo
      - add_ssh_keys:
          fingerprints:
            - "6e:66:1e:13:b2:d4:e6:a0:d2:21:75:48:52:3a:ee:51"
      - run: make deploy.ci

workflows:
  version: 2
  build:
    jobs:
      - build
